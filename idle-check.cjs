const fs=require('fs'),assert=require('assert');const s=fs.readFileSync('game.source.html','utf8');const body=s.slice(s.indexOf('function oa('),s.indexOf('const pa=',s.indexOf('function oa(')));const run=new Function('R','enemy',`let calls=[],v=null,V=Array(11).fill(0),SZ=23,ca=[0,1,2,3,4],c=Array(7).fill('#fff'),w={fillRect(){},fillText(){}},ia=()=>{},pt=()=>[0,0],la=(...a)=>calls.push(a);${body};oa(enemy);return calls[0]`);let count=0;for(const type of [1,2,3])for(let boss=1;boss<=14;boss++){const enemy={O:10,max:10,t:type,b:boss,x:0,y:0,r:1,R:0};const a=run(.3,enemy),b=run(2,enemy);assert(Math.abs((a[4]-b[4])*23-1)<1e-9);assert(a[3]===b[3]);assert(a[1]===b[1]&&a[2]===b[2]);count++}console.log(count+' idle cases passed: 1px breathing, unchanged width/position');
